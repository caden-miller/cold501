<%= turbo_stream.replace "flash_messages" do %>
  <%= render "shared/flash" %> 
<% end %>

<%= turbo_stream.replace "photo_#{@photo.id}" do %>
  <%= render partial: 'form', locals: { photo: @photo } %>
<% end %>