
<%= link_to sanitize("&larr; Back to photos"), photos_path %>
<div class="header">
  <h1>
    Edit <%= @photo.title %>
  </h1>
</div>
<%= turbo_frame_tag dom_id(@photo) do %>
  <%= render 'form', photo: @photo %>
<% end %>

