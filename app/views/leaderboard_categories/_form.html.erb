<%= form_with(model: @leaderboard_category, local: true) do |form| %>
  <% if @leaderboard_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leaderboard_category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% @leaderboard_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_name %>
    <%= form.text_field :category_name %>
  </div>

  <div class="field">
    <%= form.label :min_points %>
    <%= form.number_field :min_points %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.color_field :color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

