<%= turbo_stream.replace "flash_messages" do %>
  <%= render "shared/flash" %> 
<% end %>

<%= turbo_stream.replace "leaderboard_category_#{@leaderboard_category.id}" do %>
  <%= render partial: 'form', locals: { leaderboard_category: @leaderboard_category } %>
<% end %>