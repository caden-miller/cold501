<%= turbo_stream.replace "flash_messages" do %>
  <%= render "shared/flash" %> 
<% end %>

<%= turbo_stream.replace "link_#{@link.id}" do %>
  <%= render partial: 'form', locals: { link: @link } %>
<% end %>