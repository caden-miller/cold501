<%= turbo_stream.replace "flash_messages" do %>
  <%= render "shared/flash" %> 
<% end %>

<%= turbo_stream.replace "user_#{@user.id}" do %>
  <%= render partial: 'form', locals: { user: @user } %>
<% end %>