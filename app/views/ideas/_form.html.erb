<%= form_with(model: idea) do |form| %>
  <% if idea.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(idea.errors.count, "error") %> prohibited this idea from being saved:</h2>

      <ul>
        <% idea.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_area :title, class: 'item-title new-title', placeholder: 'Write your title here' %>
  </div> 

  <div>
    <%= form.text_area :description, id: 'description-field', class: 'idea-desc new-desc', placeholder: "Write description here" %>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
